@using System;
@using Sandbox;
@using Sandbox.UI;
@using Facepunch.Juicebox;

@namespace Facepunch.Juicebox.UI
@attribute [StyleSheet("/ui/styles/HeaderSection.scss")]
@inherits Panel

<root>
    <div class="container is-spectate">
        @if (!string.IsNullOrEmpty(GameState.JoinCode))
        {
            <span class="subtitle">Join</span>
            <div class="spectate-code">
                <span>@GameState.JoinCode</span>
            </div>
            <span class="subtitle">tv.asset.party</span>
        }
    </div>
    <div class="container is-branding">
        <div class="branding-container">
            <img class="branding sbox" src="textures/ui/branding-sbox.png" />
            <span class="subtitle">x</span>
            <img class="branding juicebox" src="textures/ui/branding-juicebox.png" />
        </div>
    </div>
    <div class="container is-round-info">
        @if (GameState.CurrentScreen >= GameScreen.QuestionPrompt)
        {
            <div class="round-info">
                <div class="round-number">
                    <span>@GameState.RoundNumber</span>
                </div>
                <div class="round-time">
                    <span>00:00</span>
                </div>
            </div>
        }
    </div>
</root>

@code {
    protected override int BuildHash()
    {
        return HashCode.Combine(GameState.CurrentScreen, GameState.JoinCode, GameState.RoundNumber);
    }
}
